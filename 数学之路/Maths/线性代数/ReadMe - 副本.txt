还有一个是昨天马同学提到的想法，我觉得是非常好的。

矩阵的行视图，事实上是矩阵基变换，（我们统一向量都是竖着的）一个向量vec(x)和一个行视图的行向量（比如vec(a)）做内积就是dot(vec(a),vec(x)) =  vec(a).Tx ,相当于是vec(x)投影后在vec(a) 上的值。

一个行向量就是新的基。
而列视图就是线性组合了，这个比较简单。

感觉行视图的应用要更广泛一些。计算上也会更方便